#include "struc1.c"

struct serpent {
    int speed
    int direction
    int couleur 
    int taille
}

int x = 300; int y = 400; int boucle _jeu = 1 ;
void deplacement

if (serpent.direction == 'g'){
    x-=20
    RemplirRectangle(x,y,TAILLE_CARRE, TAILLE_CARRE);
}
if (serpent.direction == 'h'){
    y-=20
    RemplirRectangle(x,y,TAILLE_CARRE, TAILLE_CARRE);
} 
if (serpent.direction == 'b'){
    y-=20
    RemplirRectangle(x,y,TAILLE CARRE, TAILLE_CARRE);
}
